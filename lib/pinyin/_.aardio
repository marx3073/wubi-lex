import console;
namespace pinyin;

chars = {};
for line in ..string.lines($"\lib\pinyin\.res\chars.txt") { 
	var py,str = ..string.match(line,`(\w+)\s+(.+)`)	
	if( py ) chars[py] = str; 
}

/**intellisense(pinyin)
chars = 单字与拼音映射表
end intellisense**/
